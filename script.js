// guarda el valor del contador en una variable

// capturar el h1 del contador

// Moatrarmel valor del contador actualizado
function actualizar() {
    h1
}
// Declarar funcion suma
function sumar() {
    if (contador ==30){
        contador = 30
    }
    else{

        contador = contador + 1
    }
    actualizar()
}
// Declarar funcion resta
function restar() {
    if (contador == 0) {
        contador = 0
        
    }
    contador = contador - 1 }
// Declarar funcion reiniciar
